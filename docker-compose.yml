version: '3.8'

services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: repoguide
      POSTGRES_PASSWORD: password
      POSTGRES_DB: repoguide
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
