project:
  name: repo-onboard-assistant
services:
  - hostname: db
    type: postgresql@16

  - hostname: api
    type: python@3.12
    ports:
      - port: 8000
        httpSupport: true

  - hostname: frontend
    type: static
    ports:
      - port: 80
        httpSupport: true

